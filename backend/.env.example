# PostgreSQL connection settings for the Task Tracker backend.
# Copy this file to .env and fill in real values for your local database.

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=task_tracker

# Origin the frontend dev server runs on, used to configure CORS.
FRONTEND_ORIGIN=http://localhost:5173
